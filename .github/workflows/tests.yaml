name: Automated Tests
on:
  push:
    branches: [main, user-endpoint]
  pull_request:
    branches: [main, user-endpoint]

jobs:
  jest-tests:
    name: Jest Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run Tests
        run: npm test
